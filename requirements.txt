# ============================================================
# YOLO26 Fitness Agent — Python dependencies
# ============================================================
# Install: pip install -r requirements.txt
#
# For remote API mode (DashScope), also install:
#   pip install openai
# ============================================================

# --- Computer Vision ---
numpy>=1.24.0
opencv-python>=4.8.0
ultralytics>=8.0.0
Pillow>=10.0.0

# --- Deep Learning (HuggingFace) ---
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.28.0
sentencepiece>=0.1.99

# --- LoRA Fine-tuning ---
peft>=0.8.0
trl>=0.8.0
datasets>=2.18.0
# bitsandbytes>=0.41.0   # uncomment for QLoRA (4-bit quantization)

# --- Web Backend (FastAPI) ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# --- Remote API (DashScope / Bailian) ---
openai>=1.30.0

# --- LangGraph Agent ---
langgraph>=0.2.0

# --- Data Collection (optional scrapers) ---
requests>=2.31.0

# --- Testing ---
pytest>=7.0.0
